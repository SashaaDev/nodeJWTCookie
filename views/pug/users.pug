doctype html
html
  head
    title Users List
    link(rel="stylesheet", href="style.css")
  body
    h1 Users
    ul
      each user in users
        li
          a(href="/users/" + user.id) #{user.name}
    a(href="/") Back to Home
